const fs = require('fs'); const path = require('path'); const movement = require('./services/match/middleware/movementLogic.js'); const redis = require('./services/shared/redisClient.js'); redis.get = async (k) => { if (k.includes('routing:')) return JSON.stringify({'Baiti': {next_hop: 'Baiti', cost: 10}}); return null; }; movement.calculatePath(12, 41, 'Baiti', 12, 41).then(console.log).catch(console.error); setTimeout(()=>process.exit(0), 1000);
